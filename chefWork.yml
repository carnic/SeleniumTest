---
 -  hosts: mine
    any_errors_fatal: true
    
    
    tasks:

     - name: Include variables
       include_vars: variables.yml
     
     #- name: Env variables
     #  environment:
     #    http_proxy: '{{PROXY_HTTP}}'
     #    https_proxy: '{{PROXY_HTTPS}}'
     
     - name: Copy config files
       copy: src=scripts/sshSetup.sh dest=/var/tmp/sshSetup.sh owner=root group=root mode=0777
     
     - name: Copy config files
       copy: src=svncred.json dest='{{CHEF_REPO_LOC}}'/svncred.json owner=root group=root mode=0777

     - name: Configure staging container and run the selenium tool
       script: chefWork.sh '{{ TEST_APP_SERVER }}' '{{ DATA_NODE_1 }}' '{{ DATA_NODE_2 }}' '{{PROXY_HTTP}}' '{{CHEF_REPO_LOC}}'
     
     #- name: Testing apache2 started
     #  shell: service apache2 status
     #  register: AppResult
     
     #- name: Check apache2 status
     #  fail: msg='apache2 is not running'
     #  when: "'apache2 is not running' in AppResult.stdout"
